---
# defaults file for mapr_elasticsearch

elasticsearch_version: 2.3.3
elasticsearch_build: "*"
elasticsearch_port: 9200
# Assumes POSIX client
# elasticsearch_data_home: /mapr/{{discovered_cluster_name.stdout}}/apps/elasticsearch

