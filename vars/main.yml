---
# defaults file for mapr_elasticsearch

elasticsearch_version: 2.3.3
elasticsearch_build: "*"
# Assumes POSIX client
elasticsearch_data_home: /mapr/{{discovered_cluster_name.stdout}}/apps/elasticsearch/{{inventory_hostname}}

